web:
  build: .
  ports:
    - "5068:4567"
  links:
    - mongo
  environment:
    MONGOHQ_URL: mongodb://mongo/universe
  command: rerun 'rackup --host 0.0.0.0 --port 4567'
  volumes:
    - ".:/app"
mongo:
  image: mongo
  volumes:
    - ".docker/mongo:/data/db"
